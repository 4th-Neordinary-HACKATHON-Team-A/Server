package com.rememory.service.dalle;

import com.rememory.dto.DalleDto;
import com.rememory.dto.DalleDto.DalleRequestDto;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class DalleClientTest {
    /*
    private DalleClient dalleClient;

    @Test
    @DisplayName("dalle test")
    void dalleTest() {

        // given
        DalleRequestDto request
                = DalleRequestDto
                .builder()
                .prompt("이것은 테스트입니다. 안녕하새용!")
                .size("1024x1024")
                .build();
        // when

        DalleDto.DalleResponseDto result
                = this.dalleClient.createImage(request);
        // then
        System.out.println(result);
    }

     */
}